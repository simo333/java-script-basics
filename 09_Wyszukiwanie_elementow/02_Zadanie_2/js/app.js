const divs = document.querySelectorAll("div");
console.log("Divs: " + divs.length);

const classOffers = document.querySelectorAll(".offers");
console.log(".offers: " + classOffers.length);

const hrefAttr = document.querySelectorAll("[href]");
console.log("hrefs: " + hrefAttr.length);