const liNavNavigation = document.querySelectorAll("nav.navigation li ");
console.log(liNavNavigation);

const divParagraphs = document.querySelectorAll("div p");
console.log(divParagraphs);

const articleDivs = document.querySelectorAll("article > div");
console.log(articleDivs);